---
  releases: 
  - name: promscale
    version: 1.0.3

  addons:
  - name: promscale
    release: 1.0.3
    jobs:
    - name: promscale
      release: promscale 
      properties:
        promscale: 
          db-host: "10.33.160.100"
          db-port: 5524
          db-name: timescale
          db-password: "postgres"
          db-user: postgres
          advisory-lock-id: 1
          prometheus-timeout: 15s
    
    include:
      deployments:
        - test
      name: test
      release: prometheus2
